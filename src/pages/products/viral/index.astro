---
import Layout from "../../../layouts/Layout.astro";
import { getCollection } from "astro:content";

const pandapseries = await getCollection("viral");
---

<Layout>
  <!-- Hero Banner Section (No Animations) -->
  <section class="relative h-[45vh] md:h-[55vh] company-hero hero-section scroll-animate" data-animation="fade-up">
      <!-- Collage Background -->
      <div class="absolute inset-0 z-0">
          <img 
          src="/images/aboutus-background.png" 
          alt="Modern office space with people working" 
          class="w-full h-full object-cover hero-image-reveal"
        />
        <div class="absolute inset-0 bg-black/40"></div>
      </div>
      <!-- Hero Content -->
      <div class="relative z-10 flex flex-col items-center justify-center w-full h-full">
        <h1 class="text-4xl md:text-6xl font-light text-white mb-4 tracking-wide hero-title-animate">PT Camera</h1>
        <div class="w-24 h-1 bg-teal-400 rounded mx-auto mb-2 hero-divider-animate"></div>
      </div>
    </section>

  <!-- Introduction Section -->
  <section class="introduction section-animate">
    <div class="container">
      <div class="intro-content">
        <div class="intro-text">
          <h2>Vari-focal Cameras</h2>
          <p>
            The products in the Vari-focal Cameras are highly favored for entry-level applications, including residential and small retail settings. This series is made up of the PandaBasic and PandaStar lines. Their primary business value lies in delivering outstanding performance while maintaining an affordable price point.
          </p>
        </div>
        <div class="intro-image">
          <img src="/images/banner/panda banner (1).png" alt="Residential Street View" />
        </div>
      </div>
    </div>
  </section>

  <!-- Advantage Section -->
  <section class="advantage section-animate">
    <div class="container">
      <div class="advantage-content">
        <div class="advantage-image">
          <img src="/images/banner/panda banner (2).png" alt="Retail Shopping Street" />
        </div>
        <div class="advantage-text">
          <h2>Advantage</h2>
          <p>
           All Vari-focal Cameras products come equipped with <strong>Ultra 265 compression technology</strong>, a proprietary innovation by PrimoTech. In addition, these products are designed for quick and easy installation and maintenance, allowing them to be set up almost instantly in any environment. The Panda B Series offers the most <strong>budget-friendly</strong> solution for residential and small business applications.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Technology Section -->
  <section class="technology section-animate">
    <div class="container">
      <h2 class="tech-title">Technology</h2>
      <div class="tech-row">
        <div class="tech-col">
          <div class="tech-circle">
            <svg width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="#14B8A6" stroke-width="2">
              <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            </svg>
          </div>
          <div class="tech-label">Universal Block Storage (UBS)</div>
          <div class="tech-desc">Ensure the safety of data while enhancing the efficiency.</div>
        </div>
        <div class="tech-col">
          <div class="tech-circle">
            <svg width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="#14B8A6" stroke-width="2">
              <rect x="3" y="3" width="18" height="18" rx="2" ry="2"/>
              <circle cx="8.5" cy="8.5" r="1.5"/>
              <polyline points="21,15 16,10 5,21"/>
            </svg>
          </div>
          <div class="tech-label">Vari-focal Cameras Security Technology</div>
          <div class="tech-desc">Radically reduce blurring, deliver clear, bright and colorful image.</div>
        </div>
        <div class="tech-col">
          <div class="tech-circle">
            <svg width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="#14B8A6" stroke-width="2">
              <rect x="4" y="4" width="16" height="16" rx="2"/>
              <rect x="9" y="9" width="6" height="6"/>
              <path d="m15 2-3 3-3-3"/>
            </svg>
          </div>
          <div class="tech-label">Ultra 265 Compression Technology</div>
          <div class="tech-desc">Reduce up to 95% bandwidth and storage space without compromise on image details.</div>
        </div>
      </div>
    </div>
  </section>

  <!-- Product List Section -->
  <section class="products section-animate">
    <div class="container">
      <h2>Vari-focal Cameras Product List</h2>
      <div class="product-grid">
        {pandapseries.map(item => (
          <div class="product-card">
            <a href={`/products/viral/${item.slug}`} class="product-link">
              <div class="product-image">
                <img 
                  src={item.data.image ?? (item.data.images?.[0]?.url ?? "/images/placeholder-camera.jpg")}
                  alt={item.data.images?.[0]?.caption ?? item.data.title ?? item.slug}
                />
              </div>
              <div class="product-info">
                <h3 class="product-title">{item.data.title ?? item.slug}</h3>
                <p class="product-description">
                  {item.data.description ?? ""}
                </p>
              </div>
            </a>
          </div>
        ))}
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Global Styles */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }

  /* Hero Banner - No Animations */
  .company-hero {
    /* No animation styles */
  }

  /* Section Animations */
  .section-animate {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s ease-out;
  }

  .section-visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* Introduction Section */
  .introduction {
    padding: 4rem 0;
    background: #f8fafc;
  }
  .intro-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
  }
  .intro-text h2 {
    font-size: 2.5rem;
    color: #2d3748;
    margin: 0 0 1.5rem 0;
    font-weight: 600;
  }
  .intro-text p {
    font-size: 1.1rem;
    line-height: 1.7;
    color: #4a5568;
    margin: 0;
  }
  .intro-image img {
    width: 100%;
    height: 300px;
    object-fit: cover;
    border-radius: 12px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
  }

  /* Advantage Section */
  .advantage {
    padding: 4rem 0;
  }
  .advantage-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: center;
  }
  .advantage-text h2 {
    font-size: 2.5rem;
    color: #2d3748;
    margin: 0 0 1.5rem 0;
    font-weight: 600;
  }
  .advantage-text p {
    font-size: 1.1rem;
    line-height: 1.7;
    color: #4a5568;
    margin: 0;
  }
  .advantage-text strong {
    color:  #14B8A6;
    font-weight: 600;
  }
  .advantage-image img {
    width: 100%;
    height: 300px;
    object-fit: cover;
    border-radius: 12px;
    box-shadow: 0 10px 30px rgba(0,0,0,0.1);
  }

  /* Technology Section */
  .technology {
    padding: 4rem 0 3rem 0;
    background: #fff;
  }
  .tech-title {
    text-align: center;
    font-size: 2.5rem;
    color: #22223b;
    font-weight: 500;
    margin-bottom: 2.5rem;
    letter-spacing: 0.5px;
  }
  .tech-row {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    gap: 2.5rem;
    margin: 0 auto;
    max-width: 1100px;
  }
  .tech-col {
    flex: 1 1 0;
    min-width: 220px;
    max-width: 340px;
    text-align: center;
  }
  .tech-circle {
    width: 110px;
    height: 110px;
    border: 2px solid  #14B8A6;
    border-radius: 50%;
    margin: 0 auto 1.5rem auto;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #fff;
    transition: box-shadow 0.3s;
  }
  .tech-col:hover .tech-circle {
    box-shadow: 0 4px 24px  #14B8A6;
  }
  .tech-label {
    font-size: 1.15rem;
    font-weight: 500;
    color:  #14B8A6;
    margin-bottom: 0.5rem;
    margin-top: 0.5rem;
  }
  .tech-desc {
    color: #444;
    font-size: 1rem;
    margin: 0 auto;
    max-width: 260px;
    line-height: 1.5;
  }

  /* Products Section */
  .products {
    padding: 4rem 0;
  }
  .products h2 {
    text-align: center;
    font-size: 2.5rem;
    color: #2d3748;
    margin: 0 0 3rem 0;
    font-weight: 600;
  }
  .product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 2rem;
  }
  .product-card {
    background: white;
    border-radius: 12px;
    box-shadow: 0 5px 15px rgba(0,0,0,0.08);
    overflow: hidden;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }
  .product-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 35px rgba(0,0,0,0.15);
  }
  .product-link {
    display: block;
    text-decoration: none;
    color: inherit;
  }
  .product-image {
    height: 220px;
    background: #f8fafc;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }
  .product-image img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    transition: transform 0.2s ease;
  }
  .product-card:hover .product-image img {
    transform: scale(1.05);
  }
  .product-info {
    padding: 1.5rem;
  }
  .product-title {
    font-size: 1.2rem;
    font-weight: 600;
    color: #2d3748;
    margin: 0 0 0.75rem 0;
    line-height: 1.3;
  }
  .product-description {
    color: #4a5568;
    line-height: 1.5;
    margin: 0;
    font-size: 0.95rem;
  }

  /* Responsive Design */
  @media (max-width: 900px) {
    .tech-row {
      flex-direction: column;
      align-items: center;
      gap: 2.5rem;
    }
    .tech-col {
      max-width: 100%;
    }
  }

  @media (max-width: 768px) {
    .intro-content,
    .advantage-content {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .intro-text h2,
    .advantage-text h2,
    .technology h2,
    .products h2 {
      font-size: 2rem;
    }
    .product-grid {
      grid-template-columns: 1fr;
    }
    .introduction,
    .advantage,
    .technology,
    .products {
      padding: 2rem 0;
    }
  }
  @media (max-width: 480px) {
    .intro-text h2,
    .advantage-text h2,
    .technology h2,
    .products h2 {
      font-size: 1.75rem;
    }
    .tech-col {
      padding: 0 1rem;
    }
  }
</style>

<script is:inline>
  document.addEventListener('DOMContentLoaded', function() {
    // Simple intersection observer for sections
    const sectionObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('section-visible');
        }
      });
    }, {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    });

    // Observe all content sections
    document.querySelectorAll('.section-animate').forEach(section => {
      sectionObserver.observe(section);
    });
    
    // Product card hover effects
    document.querySelectorAll('.product-card').forEach(card => {
      card.addEventListener('mouseenter', () => {
        card.style.transform = 'translateY(-8px)';
        card.style.boxShadow = '0 20px 40px rgba(0, 0, 0, 0.15)';
      });
      
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'translateY(0)';
        card.style.boxShadow = '0 4px 20px rgba(0, 0, 0, 0.08)';
      });
    });
  });
</script>
<script is:inline>
  // Initialize all animations when DOM is loaded
  document.addEventListener('DOMContentLoaded', function() {
    // Hero observer for banner cinematic effect
    const heroObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('hero-revealed');
          heroObserver.unobserve(entry.target);
        }
      });
    }, {
      threshold: 0.12,
      rootMargin: '0px 0px -40px 0px'
    });

    // Section observer for "coming out" effect
    const sectionObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('section-revealed');
          sectionObserver.unobserve(entry.target);
        }
      });
    }, {
      threshold: 0.18,
      rootMargin: '0px 0px -80px 0px'
    });

    // Category banner observer for parallax effect
    const bannerObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('banner-visible');
          
          // Stagger animations for inner elements
          const bannerImage = entry.target.querySelector('.banner-image');
          const bannerContent = entry.target.querySelector('.banner-content');
          
          if (bannerImage) {
            setTimeout(() => {
              bannerImage.style.transform = 'scale(1)';
              bannerImage.style.opacity = '1';
            }, 200);
          }
          
          if (bannerContent) {
            setTimeout(() => {
              bannerContent.style.opacity = '1';
              bannerContent.style.transform = 'translateY(0)';
            }, 400);
          }
        }
      });
    }, {
      threshold: 0.2,
      rootMargin: '0px 0px -100px 0px'
    });

    // Observe all elements
    const heroSection = document.querySelector('.company-hero');
    if (heroSection) heroObserver.observe(heroSection);

    document.querySelectorAll('.main-section').forEach(section => {
      sectionObserver.observe(section);
    });

    document.querySelectorAll('.category-banner').forEach(banner => {
      bannerObserver.observe(banner);
    });

    // Product card hover effects
    document.querySelectorAll('.product-card').forEach(card => {
      card.addEventListener('mouseenter', () => {
        card.style.transform = 'translateY(-8px)';
        card.style.boxShadow = '0 20px 40px rgba(0, 0, 0, 0.15)';
      });
      
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'translateY(0)';
        card.style.boxShadow = '0 4px 20px rgba(0, 0, 0, 0.08)';
      });
    });

    // More button hover effects
    document.querySelectorAll('.more-btn').forEach(button => {
      button.addEventListener('mouseenter', () => {
        button.style.transform = 'translateY(-2px)';
        button.style.boxShadow = '0 6px 20px rgba(20, 184, 166, 0.4)';
      });
      
      button.addEventListener('mouseleave', () => {
        button.style.transform = 'translateY(0)';
        button.style.boxShadow = '0 4px 15px rgba(20, 184, 166, 0.3)';
      });
    });
  });
</script>

<style>
  /* Base Styles */
  .min-h-screen {
    min-height: 100vh;
  }
  
  .container {
    width: 100%;
    margin-right: auto;
    margin-left: auto;
    padding-right: 1.5rem;
    padding-left: 1.5rem;
  }
  
  @media (min-width: 640px) {
    .container {
      max-width: 640px;
    }
  }
  
  @media (min-width: 768px) {
    .container {
      max-width: 768px;
    }
  }
  
  @media (min-width: 1024px) {
    .container {
      max-width: 1024px;
    }
  }
  
  @media (min-width: 1280px) {
    .container {
      max-width: 1280px;
    }
  }
  
  @media (min-width: 1536px) {
    .container {
      max-width: 1536px;
    }
  }
.company-hero {
    opacity: 0;
    transform: translateY(60px);
    transition: all 1.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .company-hero.hero-revealed {
    opacity: 1;
    transform: translateY(0);
  }
  .hero-image-reveal {
    transform: scale(1.08);
    opacity: 0;
    transition: all 2.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .company-hero.hero-revealed .hero-image-reveal {
    transform: scale(1);
    opacity: 1;
    transition-delay: 0.2s;
  }
  .hero-overlay-fade {
    opacity: 0;
    transition: opacity 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .company-hero.hero-revealed .hero-overlay-fade {
    opacity: 1;
    transition-delay: 0.5s;
  }
  .hero-content-reveal {
    opacity: 0;
    transform: translateY(40px);
    transition: all 1.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .company-hero.hero-revealed .hero-content-reveal {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.7s;
  }
  .hero-title-animate,
  .hero-divider-animate,
  .hero-subtitle-animate {
    opacity: 0;
    transform: translateY(30px);
    transition: all 1s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  .company-hero.hero-revealed .hero-title-animate {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 1s;
  }
  .company-hero.hero-revealed .hero-divider-animate {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 1.2s;
  }
  .company-hero.hero-revealed .hero-subtitle-animate {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 1.4s;
  }

  /* Section Animations */
  .main-section {
    opacity: 0;
    transform: translateY(60px) scale(0.98);
    transition: all 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    will-change: opacity, transform;
  }
  
  .main-section.section-revealed {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  /* Category Banner Styles */
  .category-banner {
    opacity: 0;
    transition: opacity 0.8s ease-out;
  }
  
  .category-banner.banner-visible {
    opacity: 1;
  }
  
  .banner-inner {
    perspective: 1000px;
    position: relative;
    border-radius: 1rem;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  }
  
  .banner-image {
    width: 100%;
    height: 24rem;
    object-fit: cover;
    transform: scale(1.1);
    opacity: 0;
    transition: all 1.2s cubic-bezier(0.22, 1, 0.36, 1);
    will-change: transform, opacity;
  }
  
  .banner-content {
    position: relative;
    z-index: 10;
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
    transition-delay: 0.3s;
    will-change: transform, opacity;
  }
  
  /* Gradient backgrounds for different categories */
  #wifi-cameras .banner-inner {
    background: linear-gradient(135deg, #0ea5e9 0%, #3b82f6 100%);
  }
  
  #ip-vr .banner-inner {
    background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
  }
  
  #analog .banner-inner {
    background: linear-gradient(135deg, #8b5cf6 0%, #d946ef 100%);
  }
  
  #transmission .banner-inner {
    background: linear-gradient(135deg, #d946ef 0%, #ec4899 100%);
  }
  
  #accessories .banner-inner {
    background: linear-gradient(135deg, #ec4899 0%, #f43f5e 100%);
  }

  /* Product Grid Styles */
  .camera-showcase {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px 0;
  }

  .product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px;
    padding: 0 20px;
  }

  .product-card {
    background: white;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    border: 1px solid rgba(0, 0, 0, 0.05);
  }

  .product-card:hover {
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    transform: translateY(-8px);
  }

  .product-image {
    position: relative;
    height: 200px;
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .product-image img {
    width: 120px;
    height: 120px;
    object-fit: contain;
    transition: transform 0.3s ease;
  }

  .product-card:hover .product-image img {
    transform: scale(1.05);
  }

  .product-info {
    padding: 24px;
    text-align: center;
  }

  .product-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: #1f2937;
    margin-bottom: 8px;
  }

  .product-description {
    font-size: 0.9rem;
    color: #6b7280;
    line-height: 1.5;
    margin-bottom: 20px;
  }

  .more-btn {
    background: linear-gradient(135deg, #14b8a6 0%, #0d9488 100%);
    color: white;
    border: none;
    padding: 12px 32px;
    border-radius: 25px;
    font-weight: 500;
    font-size: 0.9rem;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(20, 184, 166, 0.3);
  }

  .more-btn:hover {
    background: linear-gradient(135deg, #0d9488 0%, #0f766e 100%);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(20, 184, 166, 0.4);
  }

  .more-btn:active {
    transform: translateY(0);
  }

  /* Features Section Styles */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .feature-icon {
    width: 4rem;
    height: 4rem;
    background-color: #f0fdf4;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
  }

  /* Responsive Adjustments */
  @media (max-width: 768px) {
    /* Hero Section */
    .hero-title,
    .text-5xl, .text-7xl, .text-8xl {
      font-size: 2.5rem;
    }
    
    /* Category Banners */
    .category-banner {
      padding-top: 3rem;
      padding-bottom: 3rem;
    }
    
    .banner-image {
      height: 70vh;
      min-height: 300px;
    }
    
    .banner-content h2 {
      font-size: 2rem;
    }
    
    .banner-content p {
      font-size: 1rem;
    }
    
    /* Product Grid */
    .product-grid {
      grid-template-columns: 1fr;
      gap: 20px;
      padding: 0 10px;
    }
    
    .product-card {
      margin: 0 auto;
      max-width: 350px;
    }
    
    .product-info {
      padding: 20px;
    }
    
    .product-image {
      height: 180px;
    }
    
    .product-image img {
      width: 100px;
      height: 100px;
    }
    
    /* Features Section */
    .features-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 480px) {
    /* Product Grid */
    .product-grid {
      padding: 0 5px;
    }
    
    .product-card {
      max-width: 100%;
    }
    
    /* Hero Section */
    .hero-title,
    .text-5xl, .text-7xl, .text-8xl {
      font-size: 2rem;
    }
  }
</style>

<style>
  .navigation-menu {
    backdrop-filter: blur(10px);
    background-color: rgba(255, 255, 255, 0.95);
  }
  
  .nav-link {
    position: relative;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    text-decoration: none;
  }
  
  .nav-link:hover {
    background-color: rgba(20, 184, 166, 0.1);
  }
  
  .nav-link.active {
    color: #0d9488;
    background-color: rgba(20, 184, 166, 0.1);
  }
  
  .nav-link.active::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 50%;
    transform: translateX(-50%);
    width: 20px;
    height: 2px;
    background-color: #0d9488;
    border-radius: 1px;
  }
  
  @media (max-width: 768px) {
    .navigation-menu nav {
      gap: 1rem;
    }
    
    .nav-link {
      font-size: 0.9rem;
      padding: 0.4rem 0.8rem;
    }
  }
</style>
<style>
  /* Enhanced Section Animations */
  .section-animate {
    opacity: 0;
    transition: all 1.2s cubic-bezier(0.16, 1, 0.3, 1);
  }


  /* Introduction - Slide from left */
  .introduction.section-animate {
    transform: translateX(-50px);
  }
  
  .introduction.section-visible {
    opacity: 1;
    transform: translateX(0);
  }

  /* Advantage - Slide from right */
  .advantage.section-animate {
    transform: translateX(50px);
  }
  
  .advantage.section-visible {
    opacity: 1;
    transform: translateX(0);
  }

  /* Technology - Scale up */
  .technology.section-animate {
    transform: scale(0.98);
  }
  
  .technology.section-visible {
    opacity: 1;
    transform: scale(1);
  }

  /* Products - Staggered fade up */
  .products.section-animate .product-card {
    opacity: 0;
    transform: translateY(20px);
    transition: all 0.8s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  
  .products.section-visible .product-card {
    opacity: 1;
    transform: translateY(0);
  }

  /* Stagger delays for product cards */
  .products.section-visible .product-card:nth-child(1) { transition-delay: 0.1s; }
  .products.section-visible .product-card:nth-child(2) { transition-delay: 0.2s; }
  .products.section-visible .product-card:nth-child(3) { transition-delay: 0.3s; }
  .products.section-visible .product-card:nth-child(4) { transition-delay: 0.4s; }
  .products.section-visible .product-card:nth-child(5) { transition-delay: 0.5s; }
  .products.section-visible .product-card:nth-child(6) { transition-delay: 0.6s; }

  /* Technology items - Individual animations */
  .section-animate .tech-col {
    opacity: 0;
    transform: translateY(15px);
    transition: all 0.8s cubic-bezier(0.175, 0.885, 0.32, 1.275);
  }
  
  .technology.section-visible .tech-col {
    opacity: 1;
    transform: translateY(0);
  }
  body {
  overflow-x: hidden;
}

  /* Stagger the tech items */
  .technology.section-visible .tech-col:nth-child(1) { transition-delay: 0.2s; }
  .technology.section-visible .tech-col:nth-child(2) { transition-delay: 0.4s; }
  .technology.section-visible .tech-col:nth-child(3) { transition-delay: 0.6s; }
</style>

<script is:inline>
  document.addEventListener('DOMContentLoaded', function() {
    // Single intersection observer for all animations
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('section-visible');
        }
      });
    }, {
      threshold: 0.15,
      rootMargin: '0px 0px -100px 0px'
    });

    // Observe all animated sections
    document.querySelectorAll('.section-animate').forEach(section => {
      observer.observe(section);
    });
    
    // Enhanced product card hover effects
    document.querySelectorAll('.product-card').forEach(card => {
      card.addEventListener('mouseenter', () => {
        card.style.transform = 'translateY(-8px) scale(1.02)';
        card.style.boxShadow = '0 20px 40px rgba(0, 0, 0, 0.15)';
      });
      
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'translateY(0) scale(1)';
        card.style.boxShadow = '0 5px 15px rgba(0, 0, 0, 0.08)';
      });
    });
  });
</script>