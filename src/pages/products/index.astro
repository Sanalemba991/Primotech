---
import Layout from '../../layouts/Layout.astro';
import Whatsapp from '../../components/Whatsapp.tsx';
import Products from '~/components/Products.astro';

const title = "Products - PrimoTech LLC | Security Camera Solutions";
const description = "Explore PrimoTech LLC's innovative security camera products including Panda P, S, B Series and Indoor Security Cameras. Advanced IoT solutions for comprehensive security.";
const canonicalURL = new URL("/products", Astro.site);
// WiFi Camera Products Data
const wifiCameras = [
  {
    title: 'Panda WiFi Series',
    subtitle: 'Wireless Security Solutions',
    description: 'High-performance wireless cameras with easy installation and flexible placement options.',
    features: [
      '1080p/4K HD Resolution',
      'Wire-Free Installation',
      'Smart Motion Detection',
      'Two-Way Audio',
      'Cloud & Local Storage'
    ],
    image: '/images/wifi-camera-banner.jpg'
  }
];

// Camera Products Data
const wifiProducts = [
  {
    id: 'panda-p',
    title: 'Panda P Series',
    description: 'Professional PTZ cameras with advanced tracking and high-resolution imaging',
    image: '/images/pandap/PandaP (2).png',
    features: [
      '360° Pan & 90° Tilt',
      '4K Ultra HD Resolution',
      'Auto Tracking',
      'Weatherproof IP66',
      'Night Vision'
    ],
    link: '/products/pandap'
  },
  {
    id: 'panda-s',
    title: 'Panda S Series',
    description: 'Smart security cameras with AI-powered detection and cloud connectivity',
    image: '/images/pandas/PandaS (1).png',
    features: [
      'AI Person Detection',
      'Smart Alerts',
      'Two-Way Audio',
      'Wireless Connectivity',
      'Weather Resistant'
    ],
    link: '/products/pandas'
  },
  {
    id: 'panda-b',
    title: 'Panda B Series',
    description: 'Versatile bullet cameras for outdoor surveillance with weather resistance',
    image: '/images/pandab/PandaB (2).png',
    features: [
      'Varifocal Lens',
      'IP67 Weatherproof',
      'Infrared Night Vision',
      'Wide Dynamic Range',
      'Vandal Proof'
    ],
    link: '/products/pandab'
  },
  {
    id: 'indoor',
    title: 'Indoor Security Camera',
    description: 'Compact indoor cams with audio and app control',
    image: '/images/indoor/indoor (4).png',
    features: [
      'Privacy Mode',
      'Baby/Pet Monitoring',
      'Crystal Clear Audio',
      'Smart Home Integration',
      'Easy Installation'
    ],
    link: '/products/indoor'
  },
  {
    id: 'outdoor',
    title: 'Outdoor Security Camera',
    description: 'Weather-resistant outdoor cameras with night vision capabilities',
    image: '/images/outdoor/outdoor (1).png',
    features: [
      'IP65 Weatherproof',
      'Starlight Night Vision',
      'Wide Viewing Angle',
      'Smart Motion Zones',
      'Tamper Detection'
    ],
    link: '/products/outdoor'
  }
];

const IPandVR = [
  {
    id: 'dual-light',
    title: 'Dual Light Rainbow',
    description: 'Professional cameras with dual light technology for superior night vision',
    image: '/images/dual/duallight.png',
    features: [
      'Full Color Night Vision',
      '4K Ultra HD Resolution',
      'Smart IR Illumination',
      'IP67 Weatherproof',
      'AI-Powered Detection'
    ],
     link: '/products/duallight'
  },
  {
    id: 'pt-camera',
    title: 'PT Cameras',
    description: 'PTZ cameras with advanced tracking, zoom, and remote control features',
    image: '/images/pt/pt.png',
    features: [
      '360° Continuous Rotation',
      '25x Optical Zoom',
      'Auto Tracking',
      'Preset Positions',
      'Two-Way Audio'
    ], link: '/products/pt'
  },
  {
    id: 'vari-focal',
    title: 'Vari-focal Cameras',
    description: 'Cameras with adjustable focal length for flexible and versatile installation',
    image: '/images/vari/vari (9).png',
    features: [
      '2.7-12mm Motorized Lens',
      '5MP Resolution',
      'WDR Technology',
      'IP66 Rated',
      'PoE Support'
    ], link: '/products/vari'
  },
  {
    id: 'fixed-lens',
    title: 'Fixed-Lens Cameras',
    description: 'Fixed lens cameras for consistent coverage',
    image: '/images/fixed/fixed (10).png',
    features: [
      '2.8mm/3.6mm Fixed Lens',
      '4K Resolution',
      'True WDR 120dB',
      'IK10 Vandal Proof',
      'H.265 Compression'
    ], link: '/products/fixed'
  },
  {
    id: 'nvr',
    title: 'NVR Systems',
    description: 'Network Video Recorders for centralized management',
    image: '/images/nvr/NVR (1).png',
    features: [
      'Up to 32 Channels',
      '16TB Storage Capacity',
      '4K Playback',
      'Smart Search',
      'RAID Support'
    ], link: '/products/nvr'
  },
  {
    id: 'xvr',
    title: 'XVR Systems',
    description: 'Hybrid video recorders with multi-tech support',
    image: '/images/xvr/xvr (1).png',
    features: [
      'Multi-Format Support',
      'AI Channel Analysis',
      '4K Resolution',
      'HDMI/VGA Output',
      'Mobile Access'
    ], link: '/products/xvr'
  }
];

const Analog = [
  {
    id: 'analog-hd',
    title: 'Analog Fixed-lens Cameras',
    description: 'High-definition analog cameras with excellent performance',
    image: '/images/analog/322.png',
    features: [
      '5MP Resolution',
      '120dB True WDR',
      'Smart IR Technology',
      'IP67 Weatherproof',
      '3D DNR'
    ], link: '/products/analog'
  }
];

const transmission = [
  {
    id: 'switch',
    title: 'Network Switches',
    description: 'Professional PoE switches for surveillance systems',
    image: '/images/switch/switch (2).png',
    features: [
      '8/16/24 Port Options',
      'PoE+ Support',
      'Gigabit Ethernet',
      'VLAN Support',
      'Plug-and-Play'
    ],
    link: '/products/swit'
  }
];

const Accessories = [
  {
    id: 'brackets',
    title: 'Mounting Brackets',
    description: 'Professional mounting solutions for all camera types',
    image: '/images/acessories/t9.png',
    features: [
      'Adjustable Angles',
      'Weatherproof Design',
      'Universal Compatibility',
      'Stainless Steel',
      'Easy Installation'
    ], link: '/products/brackets'
  }
];
const structuredData = {
  "@context": "https://schema.org",
  "@type": "CollectionPage",
  "name": title,
  "description": description,
  "url": canonicalURL.toString(),
  "breadcrumb": {
    "@type": "BreadcrumbList",
    "itemListElement": [
      {
        "@type": "ListItem",
        "position": 1,
        "name": "Home",
        "item": Astro.site.toString(),
      },
      {
        "@type": "ListItem",
        "position": 2,
        "name": "Products",
        "item": canonicalURL.toString(),
      },
    ],
  },
  "mainEntity": {
    "@type": "ItemList",
    "itemListElement": [
      ...wifiProducts.map((product, index) => ({
        "@type": "ListItem",
        "position": index + 1,
        "item": {
          "@type": "Product",
          "name": product.title,
          "description": product.description,
          "url": new URL(product.link, Astro.site).toString(),
          "image": new URL(product.image, Astro.site).toString(),
          "aggregateRating": {
            "@type": "AggregateRating",
            "ratingValue": "4.5",
            "reviewCount": "24",
          },
          "review": {
            "@type": "Review",
            "reviewRating": {
              "@type": "Rating",
              "ratingValue": "5",
              "bestRating": "5",
            },
            "author": {
              "@type": "Person",
              "name": "Jaden Smith",
            },
            "reviewBody": "Excellent quality and reliable product.",
          },
        },
      })),
      ...IPandVR.map((product, index) => ({
        "@type": "ListItem",
        "position": wifiProducts.length + index + 1,
        "item": {
          "@type": "Product",
          "name": product.title,
          "description": product.description,
          "url": new URL(product.link, Astro.site).toString(),
          "image": new URL(product.image, Astro.site).toString(),
          "aggregateRating": {
            "@type": "AggregateRating",
            "ratingValue": "4.5",
            "reviewCount": "24",
          },
          "review": {
            "@type": "Review",
            "reviewRating": {
              "@type": "Rating",
              "ratingValue": "5",
              "bestRating": "5",
            },
            "author": {
              "@type": "Person",
              "name": "Jaden Smith",
            },
            "reviewBody": "Excellent quality and reliable product.",
          },
        },
      })),
      ...Analog.map((product, index) => ({
        "@type": "ListItem",
        "position": wifiProducts.length + IPandVR.length + index + 1,
        "item": {
          "@type": "Product",
          "name": product.title,
          "description": product.description,
          "url": new URL(product.link, Astro.site).toString(),
          "image": new URL(product.image, Astro.site).toString(),
          "aggregateRating": {
            "@type": "AggregateRating",
            "ratingValue": "4.5",
            "reviewCount": "24",
          },
          "review": {
            "@type": "Review",
            "reviewRating": {
              "@type": "Rating",
              "ratingValue": "5",
              "bestRating": "5",
            },
            "author": {
              "@type": "Person",
              "name": "Jane Smith",
            },
            "reviewBody": "Excellent quality and reliable product.",
          },
        },
      })),
      ...transmission.map((product, index) => ({
        "@type": "ListItem",
        "position": wifiProducts.length + IPandVR.length + Analog.length + index + 1,
        "item": {
          "@type": "Product",
          "name": product.title,
          "description": product.description,
          "url": new URL(product.link, Astro.site).toString(),
          "image": new URL(product.image, Astro.site).toString(),
          "aggregateRating": {
            "@type": "AggregateRating",
            "ratingValue": "4.5",
            "reviewCount": "24",
          },
          "review": {
            "@type": "Review",
            "reviewRating": {
              "@type": "Rating",
              "ratingValue": "5",
              "bestRating": "5",
            },
            "author": {
              "@type": "Person",
              "name": "Jane Smith",
            },
            "reviewBody": "Excellent quality and reliable product.",
          },
        },
      })),
      ...Accessories.map((product, index) => ({
        "@type": "ListItem",
        "position": wifiProducts.length + IPandVR.length + Analog.length + transmission.length + index + 1,
        "item": {
          "@type": "Product",
          "name": product.title,
          "description": product.description,
          "url": new URL(product.link, Astro.site).toString(),
          "image": new URL(product.image, Astro.site).toString(),
          "aggregateRating": {
            "@type": "AggregateRating",
            "ratingValue": "4.5",
            "reviewCount": "24",
          },
          "review": {
            "@type": "Review",
            "reviewRating": {
              "@type": "Rating",
              "ratingValue": "5",
              "bestRating": "5",
            },
            "author": {
              "@type": "Person",
              "name": "Jane Smith",
            },
            "reviewBody": "Excellent quality and reliable product.",
          },
        },
      })),
    ],
  },
};
---
<Layout title={title} description={description} structuredData={structuredData}>
  <main class="min-h-screen">
    
    <!-- Hero Section -->
    <section class="relative h-[45vh] md:h-[55vh] overflow-hidden company-hero hero-section scroll-animate" data-animation="fade-up">
      <!-- Collage Background -->
      <div class="absolute inset-0 z-0">
          <img 
          src="/images/aboutus-background.png" 
          alt="Modern office space with people working" 
          class="w-full h-full object-cover hero-image-reveal"
        />
        <div class="absolute inset-0 bg-black/40"></div>
      </div>
      <!-- Hero Content -->
      <div class="relative z-10 flex flex-col items-center justify-center w-full h-full">
        <h1 class="text-4xl md:text-6xl font-light text-white mb-4 tracking-wide hero-title-animate">Products</h1>
        <div class="w-24 h-1 bg-teal-400 rounded mx-auto mb-2 hero-divider-animate"></div>
      </div>
    </section>
  <section class="navigation-menu py-4 bg-white border-b border-gray-200 sticky top-16 z-30 shadow-sm">
  <div class="container mx-auto px-6 lg:px-8">
    <nav class="flex flex-wrap justify-center gap-4 md:gap-8">
      <a href="#wifi-cameras" class="nav-link text-gray-700 hover:text-teal-600 font-medium transition-all duration-300 px-3 py-2">
        WiFi Camera
      </a>
      <a href="#ip-vr" class="nav-link text-gray-700 hover:text-teal-600 font-medium transition-all duration-300 px-3 py-2">
        IP Cameras & NVRs 
        
      </a>
      <a href="#analog" class="nav-link text-gray-700 hover:text-teal-600 font-medium transition-all duration-300 px-3 py-2">
        Analog Cameras
      </a>
      <a href="#transmission" class="nav-link text-gray-700 hover:text-teal-600 font-medium transition-all duration-300 px-3 py-2">
        Transmission
      </a>
      <a href="#accessories" class="nav-link text-gray-700 hover:text-teal-600 font-medium transition-all duration-300 px-3 py-2">
        Accessories
      </a>
    </nav>
  </div>
</section>
    <!-- WiFi Camera Banner Section -->
    <section class="category-banner py-16 bg-gradient-to-r from-teal-50 to-blue-50" id="wifi-cameras">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="relative rounded-xl overflow-hidden shadow-lg banner-inner">
          <img 
            src="https://images.unsplash.com/photo-1516035069371-29a1b244cc32?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1632&q=80" 
            alt="WiFi Security Camera Banner" 
            class="w-full h-96 object-cover banner-image"
          />
          <div class="absolute inset-0 bg-gradient-to-r from-black/60 to-black/40 flex items-center justify-center">
            <div class="text-center px-6 banner-content">
              <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4">
                WiFi Camera Solutions
              </h2>
              <p class="text-xl text-white mb-6 max-w-2xl mx-auto">
                Cutting-edge wireless surveillance technology for flexible installation and smart monitoring
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- WiFi Camera Products Section -->
    <section class="main-section py-24 bg-gray-100">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-10">
          {wifiProducts.map((product) => (
            <div class="bg-white rounded-xl shadow-md flex flex-col items-center p-8 transition hover:shadow-lg">
              <div class="mb-6 flex items-center justify-center h-40">
                <img src={product.image} alt={product.title} class="h-32 object-contain" />
              </div>
              <h3 class="text-2xl font-semibold text-gray-800 mb-4 text-center">{product.title}</h3>
              <p class="text-gray-600 text-base text-center mb-6">{product.description}</p>
              <a href={product.link} class="bg-green-200 text-green-700 font-semibold px-8 py-3 rounded-full text-base transition hover:bg-green-300 focus:outline-none">More</a>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- IP & VR Banner Section -->
    <section class="category-banner py-16 bg-gradient-to-r from-blue-50 to-indigo-50" id="ip-vr">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="relative rounded-xl overflow-hidden shadow-lg banner-inner">
          <img 
            src="https://images.unsplash.com/photo-1563013544-824ae1b704d3?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1470&q=80" 
            alt="IP & VR Camera Banner" 
            class="w-full h-96 object-cover banner-image"
          />
          <div class="absolute inset-0 bg-gradient-to-r from-black/60 to-black/40 flex items-center justify-center">
            <div class="text-center px-6 banner-content">
              <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4">
                IP & VR Solutions
              </h2>
              <p class="text-xl text-white mb-6 max-w-2xl mx-auto">
                Professional-grade network video solutions with advanced recording capabilities
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- IP & VR Products Section -->
     <section class="main-section py-24 bg-gray-100">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-10">
          {IPandVR.map((product) => (
            <div class="bg-white rounded-xl shadow-md flex flex-col items-center p-8 transition hover:shadow-lg">
              <div class="mb-6 flex items-center justify-center h-40">
                <img src={product.image} alt={product.title} class="h-32 object-contain" />
              </div>
              <h3 class="text-2xl font-semibold text-gray-800 mb-4 text-center">{product.title}</h3>
              <p class="text-gray-600 text-base text-center mb-6">{product.description}</p>
             <a href={product.link} class="bg-green-200 text-green-700 font-semibold px-8 py-3 rounded-full text-base transition hover:bg-green-300 focus:outline-none">More</a>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Analog Banner Section -->
    <section class="category-banner py-16 bg-gradient-to-r from-indigo-50 to-purple-50" id="analog">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="relative rounded-xl overflow-hidden shadow-lg banner-inner">
          <img 
            src="https://images.unsplash.com/photo-1563770660941-10a63607739e?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1470&q=80" 
            alt="Analog Camera Banner" 
            class="w-full h-96 object-cover banner-image"
          />
          <div class="absolute inset-0 bg-gradient-to-r from-black/60 to-black/40 flex items-center justify-center">
            <div class="text-center px-6 banner-content">
              <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4">
                Analog Solutions
              </h2>
              <p class="text-xl text-white mb-6 max-w-2xl mx-auto">
                Reliable traditional surveillance systems with modern enhancements
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Analog Products Section -->
   <section class="main-section py-24 bg-gray-100">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-10">
          {Analog.map((product) => (
            <div class="bg-white rounded-xl shadow-md flex flex-col items-center p-8 transition hover:shadow-lg">
              <div class="mb-6 flex items-center justify-center h-40">
                <img src={product.image} alt={product.title} class="h-32 object-contain" />
              </div>
              <h3 class="text-2xl font-semibold text-gray-800 mb-4 text-center">{product.title}</h3>
              <p class="text-gray-600 text-base text-center mb-6">{product.description}</p>
             <a href={product.link} class="bg-green-200 text-green-700 font-semibold px-8 py-3 rounded-full text-base transition hover:bg-green-300 focus:outline-none">More</a>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Transmission Banner Section -->
    <section class="category-banner py-16 bg-gradient-to-r from-purple-50 to-pink-50" id="transmission">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="relative rounded-xl overflow-hidden shadow-lg banner-inner">
          <img 
            src="https://images.unsplash.com/photo-1551434678-e076c223a692?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1470&q=80" 
            alt="Transmission Equipment Banner" 
            class="w-full h-96 object-cover banner-image"
          />
          <div class="absolute inset-0 bg-gradient-to-r from-black/60 to-black/40 flex items-center justify-center">
            <div class="text-center px-6 banner-content">
              <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4">
                Transmission Equipment
              </h2>
              <p class="text-xl text-white mb-6 max-w-2xl mx-auto">
                High-performance networking solutions for seamless video transmission
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Transmission Products Section -->
    <section class="main-section py-24 bg-gray-100">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-10">
          {transmission.map((product) => (
            <div class="bg-white rounded-xl shadow-md flex flex-col items-center p-8 transition hover:shadow-lg">
              <div class="mb-6 flex items-center justify-center h-40">
                <img src={product.image} alt={product.title} class="h-32 object-contain" />
              </div>
              <h3 class="text-2xl font-semibold text-gray-800 mb-4 text-center">{product.title}</h3>
              <p class="text-gray-600 text-base text-center mb-6">{product.description}</p>
             <a href={product.link} class="bg-green-200 text-green-700 font-semibold px-8 py-3 rounded-full text-base transition hover:bg-green-300 focus:outline-none">More</a>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Accessories Banner Section -->
    <section class="category-banner py-16 bg-gradient-to-r from-pink-50 to-red-50" id="accessories">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="relative rounded-xl overflow-hidden shadow-lg banner-inner">
          <img 
            src="https://images.unsplash.com/photo-1591488320449-011701bb6704?ixlib=rb-4.0.3&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1470&q=80" 
            alt="Security Accessories Banner" 
            class="w-full h-96 object-cover banner-image"
          />
          <div class="absolute inset-0 bg-gradient-to-r from-black/60 to-black/40 flex items-center justify-center">
            <div class="text-center px-6 banner-content">
              <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4">
                Security Accessories
              </h2>
              <p class="text-xl text-white mb-6 max-w-2xl mx-auto">
                Complete your surveillance system with our premium accessories
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Accessories Products Section -->
    <section class="main-section py-24 bg-gray-100">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-10">
          {Accessories.map((product) => (
            <div class="bg-white rounded-xl shadow-md flex flex-col items-center p-8 transition hover:shadow-lg">
              <div class="mb-6 flex items-center justify-center h-40">
                <img src={product.image} alt={product.title} class="h-32 object-contain" />
              </div>
              <h3 class="text-2xl font-semibold text-gray-800 mb-4 text-center">{product.title}</h3>
              <p class="text-gray-600 text-base text-center mb-6">{product.description}</p>
            <a href={product.link} class="bg-green-200 text-green-700 font-semibold px-8 py-3 rounded-full text-base transition hover:bg-green-300 focus:outline-none">More</a>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="main-section py-24 bg-white">
      <div class="container mx-auto px-6 lg:px-8">
        <div class="text-center mb-20">
          <h2 class="text-4xl md:text-5xl font-light text-gray-900 mb-6">
            Why Choose Our Products
          </h2>
          <div class="w-32 h-0.5 bg-teal-400 mx-auto mb-8"></div>
        </div>

        <div class="grid md:grid-cols-3 gap-12">
          <div class="text-center">
            <div class="w-16 h-16 bg-teal-100 rounded-full flex items-center justify-center mx-auto mb-6">
              <svg class="w-8 h-8 text-teal-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-4">Advanced Security</h3>
            <p class="text-gray-600">Military-grade encryption and secure data transmission for complete peace of mind.</p>
          </div>

          <div class="text-center">
            <div class="w-16 h-16 bg-teal-100 rounded-full flex items-center justify-center mx-auto mb-6">
              <svg class="w-8 h-8 text-teal-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-4">AI-Powered Intelligence</h3>
            <p class="text-gray-600">Smart detection algorithms that learn and adapt to your environment for accurate alerts.</p>
          </div>

          <div class="text-center">
            <div class="w-16 h-16 bg-teal-100 rounded-full flex items-center justify-center mx-auto mb-6">
              <svg class="w-8 h-8 text-teal-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 18h.01M8 21h8a2 2 0 002-2V5a2 2 0 00-2-2H8a2 2 0 00-2 2v14a2 2 0 002 2z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-4">Mobile Integration</h3>
            <p class="text-gray-600">Seamless mobile app control with real-time notifications and remote monitoring.</p>
          </div>
        </div>
      </div>
    </section>

  </main>
    <Whatsapp client:load />
</Layout>

<script is:inline>
  // Initialize all animations when DOM is loaded
  document.addEventListener('DOMContentLoaded', function() {
    // Hero observer for banner cinematic effect
    const heroObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('hero-revealed');
          heroObserver.unobserve(entry.target);
        }
      });
    }, {
      threshold: 0.12,
      rootMargin: '0px 0px -40px 0px'
    });

    // Section observer for "coming out" effect
    const sectionObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('section-revealed');
          sectionObserver.unobserve(entry.target);
        }
      });
    }, {
      threshold: 0.18,
      rootMargin: '0px 0px -80px 0px'
    });

    // Category banner observer for parallax effect
    const bannerObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('banner-visible');
          
          // Stagger animations for inner elements
          const bannerImage = entry.target.querySelector('.banner-image');
          const bannerContent = entry.target.querySelector('.banner-content');
          
          if (bannerImage) {
            setTimeout(() => {
              bannerImage.style.transform = 'scale(1)';
              bannerImage.style.opacity = '1';
            }, 200);
          }
          
          if (bannerContent) {
            setTimeout(() => {
              bannerContent.style.opacity = '1';
              bannerContent.style.transform = 'translateY(0)';
            }, 400);
          }
        }
      });
    }, {
      threshold: 0.2,
      rootMargin: '0px 0px -100px 0px'
    });

    // Observe all elements
    const heroSection = document.querySelector('.company-hero');
    if (heroSection) heroObserver.observe(heroSection);

    document.querySelectorAll('.main-section').forEach(section => {
      sectionObserver.observe(section);
    });

    document.querySelectorAll('.category-banner').forEach(banner => {
      bannerObserver.observe(banner);
    });

    // Product card hover effects
    document.querySelectorAll('.product-card').forEach(card => {
      card.addEventListener('mouseenter', () => {
        card.style.transform = 'translateY(-8px)';
        card.style.boxShadow = '0 20px 40px rgba(0, 0, 0, 0.15)';
      });
      
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'translateY(0)';
        card.style.boxShadow = '0 4px 20px rgba(0, 0, 0, 0.08)';
      });
    });

    // More button hover effects
    document.querySelectorAll('.more-btn').forEach(button => {
      button.addEventListener('mouseenter', () => {
        button.style.transform = 'translateY(-2px)';
        button.style.boxShadow = '0 6px 20px rgba(20, 184, 166, 0.4)';
      });
      
      button.addEventListener('mouseleave', () => {
        button.style.transform = 'translateY(0)';
        button.style.boxShadow = '0 4px 15px rgba(20, 184, 166, 0.3)';
      });
    });
  });
</script>

<style>
  /* Base Styles */
  .min-h-screen {
    min-height: 100vh;
  }
  
  .container {
    width: 100%;
    margin-right: auto;
    margin-left: auto;
    padding-right: 1.5rem;
    padding-left: 1.5rem;
  }
  
  @media (min-width: 640px) {
    .container {
      max-width: 640px;
    }
  }
  
  @media (min-width: 768px) {
    .container {
      max-width: 768px;
    }
  }
  
  @media (min-width: 1024px) {
    .container {
      max-width: 1024px;
    }
  }
  
  @media (min-width: 1280px) {
    .container {
      max-width: 1280px;
    }
  }
  
  @media (min-width: 1536px) {
    .container {
      max-width: 1536px;
    }
  }
.company-hero {
    opacity: 0;
    transform: translateY(60px);
    transition: all 1.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .company-hero.hero-revealed {
    opacity: 1;
    transform: translateY(0);
  }
  .hero-image-reveal {
    transform: scale(1.08);
    opacity: 0;
    transition: all 2.2s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .company-hero.hero-revealed .hero-image-reveal {
    transform: scale(1);
    opacity: 1;
    transition-delay: 0.2s;
  }
  .hero-overlay-fade {
    opacity: 0;
    transition: opacity 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .company-hero.hero-revealed .hero-overlay-fade {
    opacity: 1;
    transition-delay: 0.5s;
  }
  .hero-content-reveal {
    opacity: 0;
    transform: translateY(40px);
    transition: all 1.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }
  .company-hero.hero-revealed .hero-content-reveal {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.7s;
  }
  .hero-title-animate,
  .hero-divider-animate,
  .hero-subtitle-animate {
    opacity: 0;
    transform: translateY(30px);
    transition: all 1s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  .company-hero.hero-revealed .hero-title-animate {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 1s;
  }
  .company-hero.hero-revealed .hero-divider-animate {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 1.2s;
  }
  .company-hero.hero-revealed .hero-subtitle-animate {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 1.4s;
  }

  /* Section Animations */
  .main-section {
    opacity: 0;
    transform: translateY(60px) scale(0.98);
    transition: all 1.5s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    will-change: opacity, transform;
  }
  
  .main-section.section-revealed {
    opacity: 1;
    transform: translateY(0) scale(1);
  }

  /* Category Banner Styles */
  .category-banner {
    opacity: 0;
    transition: opacity 0.8s ease-out;
  }
  
  .category-banner.banner-visible {
    opacity: 1;
  }
  
  .banner-inner {
    perspective: 1000px;
    position: relative;
    border-radius: 1rem;
    overflow: hidden;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
  }
  
  .banner-image {
    width: 100%;
    height: 24rem;
    object-fit: cover;
    transform: scale(1.1);
    opacity: 0;
    transition: all 1.2s cubic-bezier(0.22, 1, 0.36, 1);
    will-change: transform, opacity;
  }
  
  .banner-content {
    position: relative;
    z-index: 10;
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
    transition-delay: 0.3s;
    will-change: transform, opacity;
  }
  
  /* Gradient backgrounds for different categories */
  #wifi-cameras .banner-inner {
    background: linear-gradient(135deg, #0ea5e9 0%, #3b82f6 100%);
  }
  
  #ip-vr .banner-inner {
    background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
  }
  
  #analog .banner-inner {
    background: linear-gradient(135deg, #8b5cf6 0%, #d946ef 100%);
  }
  
  #transmission .banner-inner {
    background: linear-gradient(135deg, #d946ef 0%, #ec4899 100%);
  }
  
  #accessories .banner-inner {
    background: linear-gradient(135deg, #ec4899 0%, #f43f5e 100%);
  }

  /* Product Grid Styles */
  .camera-showcase {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 20px 0;
  }

  .product-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px;
    padding: 0 20px;
  }

  .product-card {
    background: white;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    overflow: hidden;
    transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    border: 1px solid rgba(0, 0, 0, 0.05);
  }

  .product-card:hover {
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
    transform: translateY(-8px);
  }

  .product-image {
    position: relative;
    height: 200px;
    background: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .product-image img {
    width: 120px;
    height: 120px;
    object-fit: contain;
    transition: transform 0.3s ease;
  }

  .product-card:hover .product-image img {
    transform: scale(1.05);
  }

  .product-info {
    padding: 24px;
    text-align: center;
  }

  .product-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: #1f2937;
    margin-bottom: 8px;
  }

  .product-description {
    font-size: 0.9rem;
    color: #6b7280;
    line-height: 1.5;
    margin-bottom: 20px;
  }

  .more-btn {
    background: linear-gradient(135deg, #14b8a6 0%, #0d9488 100%);
    color: white;
    border: none;
    padding: 12px 32px;
    border-radius: 25px;
    font-weight: 500;
    font-size: 0.9rem;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(20, 184, 166, 0.3);
  }

  .more-btn:hover {
    background: linear-gradient(135deg, #0d9488 0%, #0f766e 100%);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(20, 184, 166, 0.4);
  }

  .more-btn:active {
    transform: translateY(0);
  }

  /* Features Section Styles */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .feature-icon {
    width: 4rem;
    height: 4rem;
    background-color: #f0fdf4;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
  }

  /* Responsive Adjustments */
  @media (max-width: 768px) {
    /* Hero Section */
    .hero-title,
    .text-5xl, .text-7xl, .text-8xl {
      font-size: 2.5rem;
    }
    
    /* Category Banners */
    .category-banner {
      padding-top: 3rem;
      padding-bottom: 3rem;
    }
    
    .banner-image {
      height: 70vh;
      min-height: 300px;
    }
    
    .banner-content h2 {
      font-size: 2rem;
    }
    
    .banner-content p {
      font-size: 1rem;
    }
    
    /* Product Grid */
    .product-grid {
      grid-template-columns: 1fr;
      gap: 20px;
      padding: 0 10px;
    }
    
    .product-card {
      margin: 0 auto;
      max-width: 350px;
    }
    
    .product-info {
      padding: 20px;
    }
    
    .product-image {
      height: 180px;
    }
    
    .product-image img {
      width: 100px;
      height: 100px;
    }
    
    /* Features Section */
    .features-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 480px) {
    /* Product Grid */
    .product-grid {
      padding: 0 5px;
    }
    
    .product-card {
      max-width: 100%;
    }
    
    /* Hero Section */
    .hero-title,
    .text-5xl, .text-7xl, .text-8xl {
      font-size: 2rem;
    }
  }
</style><script is:inline>
  document.addEventListener('DOMContentLoaded', function() {
    // Handle navigation clicks for hash links only (do not prevent default for normal links)
    document.querySelectorAll('.nav-link').forEach(link => {
      const href = link.getAttribute('href');
      if (href && href.startsWith('#')) {
        link.addEventListener('click', function(e) {
          e.preventDefault();
          const targetId = this.getAttribute('href').substring(1);
          const targetSection = document.getElementById(targetId);
          if (targetSection) {
            // Account for sticky header height
            const headerOffset = 120;
            const elementPosition = targetSection.offsetTop;
            const offsetPosition = elementPosition - headerOffset;
            window.scrollTo({
              top: offsetPosition,
              behavior: 'smooth'
            });
          }
        });
      }
      // For normal links (not hash), do not preventDefault
    });

    // Highlight active section on scroll (only for hash links)
    const sections = document.querySelectorAll('section[id]');
    const navLinks = document.querySelectorAll('.nav-link');
    window.addEventListener('scroll', () => {
      let current = '';
      sections.forEach(section => {
        const sectionTop = section.offsetTop - 150;
        if (window.pageYOffset >= sectionTop) {
          current = section.getAttribute('id');
        }
      });
      navLinks.forEach(link => {
        const href = link.getAttribute('href');
        link.classList.remove('active');
        if (href && href.startsWith('#') && href === `#${current}`) {
          link.classList.add('active');
        }
      });
    });
  });
</script>

<style>
  .navigation-menu {
    backdrop-filter: blur(10px);
    background-color: rgba(255, 255, 255, 0.95);
  }
  
  .nav-link {
    position: relative;
    padding: 0.5rem 1rem;
    border-radius: 0.5rem;
    text-decoration: none;
  }
  
  .nav-link:hover {
    background-color: rgba(20, 184, 166, 0.1);
  }
  
  .nav-link.active {
    color: #0d9488;
    background-color: rgba(20, 184, 166, 0.1);
  }
  
  .nav-link.active::after {
    content: '';
    position: absolute;
    bottom: -2px;
    left: 50%;
    transform: translateX(-50%);
    width: 20px;
    height: 2px;
    background-color: #0d9488;
    border-radius: 1px;
  }
  
  @media (max-width: 768px) {
    .navigation-menu nav {
      gap: 1rem;
    }
    
    .nav-link {
      font-size: 0.9rem;
      padding: 0.4rem 0.8rem;
    }
  }
</style>